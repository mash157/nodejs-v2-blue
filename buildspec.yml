version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies
      - npm install

  build:
    commands:
      - echo Build Started
      - echo Build Successful

artifacts:
  files:
    - '**/*'